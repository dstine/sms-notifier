name: Build
on: [push]

jobs:
  build:
    name: Example
    runs-on: ubuntu-latest
    steps:
      - name: example
        run: echo 'Hello, everyone.'
    name: App
    runs-on: ubuntu-latest
    steps:
      - name: Gradle
        run: ./gradlew build

